[project]
name = "home-price-estimator-ml"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "AKRIT SINGHA", email = "64059503+akritsingha@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "joblib>=1.5.3",
    "pandas>=3.0.5",
    "pydantic>=2.13.4",
    "python-multipart>=0.0.32",
    "scikit-learn>=1.9.0",
    "uvicorn>=0.52.2",
]

[project.scripts]
home-price-estimator-ml = "home_price_estimator_ml:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
